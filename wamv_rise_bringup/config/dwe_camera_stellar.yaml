# Camera-specific calibration and video stream settings for the "stellarHD" model.
# The wildcard '/**' allows these parameters to be applied to any node.
/**:
  ros__parameters:
    # Video stream settings
    video:
      product_name: 'stellarHD'
      width: 1600
      height: 1200
      framerate: 15
      format: 'MJPG'
    
    # Camera calibration
    camera:
      # --- Camera Intrinsics (REQUIRED for pose estimation) ---
      # stellar air
      intrinsics:
        fx: 953.919689
        fy: 954.022011
        cx: 818.210277
        cy: 665.849871
      # Distortion coefficients. Standard model: [k1, k2, p1, p2, k3...]. Fisheye model: [k1, k2, k3, k4].
      distortion: [-0.064599, -0.002388, 0.001767, 0.000415]
      # Set to true to use the fisheye model for undistortion, false for standard (plumb bob) model.
      fisheye: True
      # Set to true to crop the undistorted image to show only valid pixels (removes black borders).
      undistort_crop: True